# Mac / CPU quickstart (no NVIDIA packages from upstream requirements.txt).
# Create env: uv venv .venv --python 3.12 && source .venv/bin/activate
# Install:    uv pip install -r requirements.txt

numpy<2
jax[cpu]>=0.4.33
chex
colored_traceback
einops
elements>=3.19.1
granular>=0.20.3
jaxtyping
ninjax>=3.5.1
optax
portal>=3.5.0
ruamel.yaml
scope>=0.4.4
tqdm

crafter
