# Install JAX for your machine first, then this file.
# Apple Silicon / CPU-only Mac:
#   pip install -U "jax[cpu]"
# Linux with NVIDIA GPU: see https://github.com/google/jax#pip-installation-gpu-cuda
#
# DreamerV3 upstream pins CUDA JAX in its own requirements.txt; this file is a
# lighter set for local dev. Full training deps: pip install -e ./dreamerv3

-e ./dreamerv3

chex
colored_traceback
einops
elements>=3.19.1
granular>=0.20.3
jaxtyping
ninjax>=3.5.1
numpy<2
optax
portal>=3.5.0
scope>=0.4.4
tqdm

# Optional: first env to verify Dreamer runs (small, no GPU)
crafter
